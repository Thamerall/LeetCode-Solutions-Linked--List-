public class RemoveLLElement_203 {
}

/*

    public ListNode removeElements(ListNode head, int data) {
        if(head == null) return null;
        head.next = removeElements(head.next, data);
        return head.val == data ? head.next : head;
    }

    */
